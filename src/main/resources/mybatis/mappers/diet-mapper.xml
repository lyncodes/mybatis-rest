<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.boot.mybatisrest.dao.Diet.DietMapper">

    <select id="getDiets" resultType="spring.boot.mybatisrest.entity.Diet">
    select * from diet where user_id = #{user_id}
</select>

</mapper>
